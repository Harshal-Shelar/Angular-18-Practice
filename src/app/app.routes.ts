import { Routes } from '@angular/router';
import { NgForAndngIfComponent } from './components/ng-for-andng-if/ng-for-andng-if.component';

export const routes: Routes = [
    {path : 'ngIfngFor', component : NgForAndngIfComponent}
];
